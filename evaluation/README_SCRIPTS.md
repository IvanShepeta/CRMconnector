# Порівняння Evaluation Скриптів

Пояснення різниці між трьома основними скриптами для тестування агента.

## 📊 Швидке порівняння

| Характеристика | `quick_test.py` | `generate_test_data.py` | `evaluate_agent.py` |
|----------------|-----------------|-------------------------|---------------------|
| **Кількість запитів** | 10 (фіксовано) | 22 (+ варіації) | 8 (фіксовано) |
| **Мета** | Швидка перевірка | Генерація даних | Детальна оцінка |
| **Час виконання** | ~30-40 сек | ~1-2 хв | ~25-35 сек |
| **Складність** | Проста | Середня | Висока |
| **Оцінка якості** | Базова | Ні | Детальна (scoring) |
| **Варіації запитів** | Ні | Так (4 варіації/запит) | Ні |
| **Використання** | Щоденне тестування | Збір датасету | Офіційна оцінка |
| **Вивід** | Консоль + JSON | JSON файл | Детальні звіти |

---

## 1. `quick_test.py` - Швидке Тестування

### 🎯 Призначення
Швидка перевірка базової функціональності агента. Для щоденного використання.

### ✨ Особливості
- **10 найважливіших запитів** що покривають основні сценарії
- **Швидке виконання** (~30-40 секунд)
- **Простий вивід** з базовою статистикою
- **Без оцінки** - просто збір відповідей

### 📝 10 запитів:
1. Пошук курсів Python
2. Курси для початківців
3. Ціна JavaScript
4. Знижки
5. Дати набору
6. Тривалість курсу
7. Запис на React
8. Програма курсу
9. Передумови
10. Подяка

### 💻 Використання
```bash
# Просто запустіть
python evaluation/quick_test.py
```

### 📤 Вихідні дані
- Консольний вивід з відповідями
- JSON файл: `evaluation/data/quick_test_TIMESTAMP.json`
- Базова статистика (час, використання інструментів)

### ✅ Коли використовувати
- Щоденна перевірка після змін
- Швидкий smoke test
- Перевірка чи агент працює
- Перед commit в Git

### ❌ Коли НЕ використовувати
- Для офіційної оцінки якості
- Для тренування моделі
- Для детального аналізу

---

## 2. `generate_test_data.py` - Генерація Даних

### 🎯 Призначення
Генерація великого датасету з варіаціями запитів та збір відповідей агента.

### ✨ Особливості
- **22 базових запити** + 4 варіації кожного = **110 запитів всього**
- **Категоризація** по типах задач
- **Метадані** для кожного запиту
- **Flexible** - можна фільтрувати по категорії, кількості

### 📝 22 базових запити:
**Course Search (5)**
- Які курси є?
- Python для початківців?
- Веб-розробка
- ...

**Pricing (3)**
- Скільки коштує?
- Знижки
- Розстрочка

**Scheduling (3)**
- Коли старт?
- Тривалість
- Розклад

**Enrollment (3)**
- Як записатися?
- Хочу записатися
- Які документи?

**Course Details (3)**
- Програма курсу
- Передумови
- Викладачі

**Certification (2)**
- Сертифікат
- Перспективи

**Comparison (2)**
- Python vs JavaScript
- Що обрати?

**Support (1)**
- Контакти

**Conversation (2)**
- Привіт
- Дякую

### 💻 Використання
```bash
# Згенерувати тільки список запитів
python evaluation/generate_test_data.py --mode generate

# Зібрати відповіді для всіх 22 запитів
python evaluation/generate_test_data.py --mode collect

# Повний цикл (генерація + збір)
python evaluation/generate_test_data.py --mode both

# Фільтрувати по категорії
python evaluation/generate_test_data.py --category pricing

# Обмежити кількість
python evaluation/generate_test_data.py --max-queries 10

# Включити всі варіації (110 запитів!)
python evaluation/generate_test_data.py --include-variations
```

### 📤 Вихідні дані
- `evaluation/data/synthetic_queries.json` - список всіх запитів
- `evaluation/data/dataset_TIMESTAMP.json` - запити + відповіді
- Детальна статистика по категоріях

### ✅ Коли використовувати
- Збір великого датасету для аналізу
- Тренування/fine-tuning моделі
- Дослідження варіацій формулювань
- A/B тестування різних версій агента
- Створення benchmark датасету

### ❌ Коли НЕ використовувати
- Для швидкої перевірки (занадто довго)
- Щоденне тестування
- Коли потрібна оцінка якості

---

## 3. `evaluate_agent.py` - Офіційна Оцінка

### 🎯 Призначення
Детальна оцінка якості агента з scoring та pass/fail критеріями.

### ✨ Особливості
- **8 тестових сценаріїв** з чіткими критеріями успіху
- **Автоматичне scoring** - кожен тест оцінюється 0-100%
- **Success criteria** - перевірка конкретних очікувань
- **Pass/Fail** - тест проходить якщо score ≥ 70%
- **Детальні звіти** з аналізом кожного критерію

### 📝 8 тестових кейсів:
1. **TC001**: Python курси (course_search)
2. **TC002**: Курси для початківців (course_search)
3. **TC003**: Ціна курсу (pricing)
4. **TC004**: Дати набору (scheduling)
5. **TC005**: Запис на курс (enrollment)
6. **TC006**: Подяка (conversation)
7. **TC007**: Програма курсу (course_details)
8. **TC008**: Передумови (course_details)

### 🎯 Success Criteria (приклади)
```python
# TC001: Python курси
success_criteria = {
    "contains_course_info": True,      # Відповідь містить інфо про курс
    "tool_used": True,                  # Використано інструмент
    "response_length_min": 50          # Мінімум 50 символів
}

# TC005: Запис на курс
success_criteria = {
    "mentions_enrollment": True,        # Згадується реєстрація
    "tool_used": True,                  # Використано інструмент
    "asks_for_details": True           # Просить деталі
}
```

### 💻 Використання
```bash
# Запустити повну оцінку
python evaluation/evaluate_agent.py
```

### 📤 Вихідні дані
- Детальний консольний вивід з scoring
- JSON звіт: `evaluation/reports/evaluation_report_TIMESTAMP.json`
- Pass/Fail для кожного тесту
- Загальна оцінка (pass rate, average score)
- Розбивка по категоріях

### 📊 Приклад виводу
```
======================================================================
Running Test: TC001 - course_search
Query: Привіт, чи є зараз курси по Python?
======================================================================
[Відповідь агента...]

──────────────────────────────────────────────────────────────────────
Result: ✓ PASS (Score: 85.00%)
Tools Called: ['local_server_crmconnector_search_courses']
Execution Time: 2.34s
Details: {
  "contains_course_info": "PASS",
  "tool_used": "PASS (tools: ['search_courses'])",
  "response_length_min": "PASS (length: 156)"
}

======================================================================
EVALUATION SUMMARY
======================================================================
Total Tests: 8
Passed: 7 | Failed: 1
Pass Rate: 87.5%
Average Score: 78.5%
```

### ✅ Коли використовувати
- Офіційна оцінка перед релізом
- Після значних змін в агенті
- Для документації якості
- Regression testing
- Порівняння різних версій агента

### ❌ Коли НЕ використовувати
- Щоденне тестування (занадто детально)
- Під час активної розробки

---

## 🔄 Рекомендований Workflow

### Щоденна розробка
```bash
# 1. Швидка перевірка після змін
python evaluation/quick_test.py

# 2. Якщо все ОК - commit
git add .
git commit -m "Updated agent instructions"
```

### Тижнева оцінка
```bash
# 1. Запустити офіційну оцінку
python evaluation/evaluate_agent.py

# 2. Проаналізувати результати
python evaluation/analyze_and_improve.py --latest

# 3. Застосувати покращення
```

### Збір датасету
```bash
# Один раз на тиждень/місяць
python evaluation/generate_test_data.py --mode both

# Зберегти для аналізу трендів
mv evaluation/data/dataset_*.json evaluation/data/archive/
```

### Перед релізом
```bash
# 1. Повна оцінка
python evaluation/evaluate_agent.py

# 2. Збір великого датасету
python evaluation/generate_test_data.py --mode collect --max-queries 50

# 3. Детальний аналіз
python evaluation/analyze_and_improve.py --latest

# 4. Якщо pass rate > 80% - готово до релізу
```

---

## 📈 Порівняння виводу

### `quick_test.py` вивід:
```json
{
  "test_name": "Quick Test - 10 Essential Queries",
  "total_queries": 10,
  "results": [...],
  "statistics": {
    "total_time": 34.5,
    "average_time": 3.45,
    "tool_usage_rate": 0.7
  }
}
```

### `generate_test_data.py` вивід:
```json
{
  "generated_at": "2025-12-30T12:00:00",
  "total_queries": 22,
  "queries": [
    {
      "id": "SQ001",
      "query": "Які курси є?",
      "variations": [...],
      "expected_intent": "list_all_courses",
      "expected_tools": [...]
    }
  ],
  "responses": [...],
  "statistics": {
    "category_breakdown": {...}
  }
}
```

### `evaluate_agent.py` вивід:
```json
{
  "timestamp": "2025-12-30T12:00:00",
  "total_tests": 8,
  "passed_tests": 7,
  "failed_tests": 1,
  "average_score": 0.85,
  "results": [
    {
      "test_id": "TC001",
      "success": true,
      "score": 0.85,
      "details": {
        "contains_course_info": "PASS",
        "tool_used": "PASS"
      }
    }
  ],
  "summary": {
    "pass_rate": "87.5%",
    "category_breakdown": {...}
  }
}
```

---

## 🎓 Висновок

**Використовуйте:**
- 🚀 `quick_test.py` - для **щоденної** перевірки (швидко, просто)
- 📊 `generate_test_data.py` - для **збору датасету** (багато запитів, варіації)
- ✅ `evaluate_agent.py` - для **офіційної оцінки** (детальний scoring, pass/fail)

**Комбінуйте всі три** для повного циклу розробки та тестування!

---

## 🔧 Додаткові скрипти

### `analyze_and_improve.py`
- Аналізує результати будь-якого з трьох скриптів
- Дає конкретні рекомендації
- Використовуйте після кожного тестування

```bash
python evaluation/analyze_and_improve.py --latest
```
